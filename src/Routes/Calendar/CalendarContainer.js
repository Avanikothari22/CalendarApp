
import CalendarComponent from './CalendarComponent'
import withContainer from '../Container';

// store functions comes here (if any store interaction required)

export default withContainer(CalendarComponent);